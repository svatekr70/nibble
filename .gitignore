node_modules/
coverage/
*.tsbuildinfo
.DS_Store
# Sestavená knihovna v /dist se commituje schválně — jde o to, aby šla načíst
# z jsDelivr přímo z repozitáře. Ostatní výstupy buildu do repozitáře nepatří.
demo/dist/
demo/fixtures/
test-results/
playwright-report/
dist-site/

# Původ vzorků: schéma cizí databáze do veřejného repozitáře nepatří.
packages/core/test/fixtures/provenance.json

# Vzorky ze schránky. Jsou v nich reálná data ze sešitů — do repozitáře nepatří.
samples/

# Stránky kolem ikon: generuje je tools/build-icons.mjs (--preview a --live).
demo/icons.html
demo/icons-live.html
